services:
  zammad-nginx:
    volumes:
      #- zammad-backup:/var/tmp/zammad:ro  # needed for waiting on restore operations
      #- zammad-storage:/opt/zammad/storage
      #- /data/docker/zammad/nginx/99-origin-fix.conf:/etc/nginx/conf.d/99-origin-fix.conf
      -  /data/docker/zammad/nginx/default:/etc/nginx/sites-enabled/default:rw
